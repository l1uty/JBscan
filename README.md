# JBscan

**Usage**

使用方法
```
usage: JBscan [-h] [-u URL] [-f FILE] [-o OUTPUT]

Usage of ./JBscan:
  -f string
        包含目标URL的文件(目标地址一行一个)
  -o string
        将扫描结果保存到文件(建议保存为html)
  -u string
        目标URL
```
**Example**
```
JBscan -f test.txt -o report.html
```
